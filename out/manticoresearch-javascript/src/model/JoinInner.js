/**
 * Manticore Search Client
 * Сlient for Manticore Search. 
 *
 * The version of the OpenAPI document: 4.0.0
 * Contact: info@manticoresearch.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';
import FulltextFilter from './FulltextFilter';
import JoinInnerOnInner from './JoinInnerOnInner';

/**
 * The JoinInner model module.
 * @module model/JoinInner
 * @version 4.0.0
 */
class JoinInner {
    /**
     * Constructs a new <code>JoinInner</code>.
     * @alias module:model/JoinInner
     * @param on {Array.<module:model/JoinInnerOnInner>} 
     * @param table {String} 
     * @param type {module:model/JoinInner.TypeEnum} 
     */
    constructor(on, table, type) { 
        
        JoinInner.initialize(this, on, table, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, on, table, type) { 
        obj['on'] = on;
        obj['table'] = table;
        obj['type'] = type;
    }

    /**
     * Constructs a <code>JoinInner</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/JoinInner} obj Optional instance to populate.
     * @return {module:model/JoinInner} The populated <code>JoinInner</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new JoinInner();

            if (data.hasOwnProperty('on')) {
                obj['on'] = ApiClient.convertToType(data['on'], [JoinInnerOnInner]);
            }
            if (data.hasOwnProperty('query')) {
                obj['query'] = FulltextFilter.constructFromObject(data['query']);
            }
            if (data.hasOwnProperty('table')) {
                obj['table'] = ApiClient.convertToType(data['table'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>JoinInner</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>JoinInner</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of JoinInner.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        if (data['on']) { // data not null
            // ensure the json data is an array
            if (!Array.isArray(data['on'])) {
                throw new Error("Expected the field `on` to be an array in the JSON data but got " + data['on']);
            }
            // validate the optional field `on` (array)
            for (const item of data['on']) {
                JoinInnerOnInner.validateJSON(item);
            };
        }
        // validate the optional field `query`
        if (data['query']) { // data not null
          FulltextFilter.validateJSON(data['query']);
        }
        // ensure the json data is a string
        if (data['table'] && !(typeof data['table'] === 'string' || data['table'] instanceof String)) {
            throw new Error("Expected the field `table` to be a primitive type in the JSON string but got " + data['table']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

JoinInner.RequiredProperties = ["on", "table", "type"];

/**
 * @member {Array.<module:model/JoinInnerOnInner>} on
 */
JoinInner.prototype['on'] = undefined;

/**
 * @member {module:model/FulltextFilter} query
 */
JoinInner.prototype['query'] = undefined;

/**
 * @member {String} table
 */
JoinInner.prototype['table'] = undefined;

/**
 * @member {module:model/JoinInner.TypeEnum} type
 */
JoinInner.prototype['type'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
JoinInner['TypeEnum'] = {

    /**
     * value: "inner"
     * @const
     */
    "inner": "inner",

    /**
     * value: "left"
     * @const
     */
    "left": "left"
};



export default JoinInner;

